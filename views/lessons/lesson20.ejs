
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>String Library Functions in C</title>
    <link rel="stylesheet" href="style/lesson1.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>

<body>
    <div class="slider">
        <div class="slide active">
            <img src="assets/bp.jpg" alt="">
            <div class="info cssanimation leFadeInLeft sequence">
                <h1>String Library Functions in C</h1>
        <p>Exploring the key functions provided by the string.h header for string manipulation in C.</p>
            </div>
        </div>


        <div class="slide">
            <img src="assets/lg.jpg" alt="">
            <div class="info">
                <h1>The <code>string.h</code> Header</h1>
                <p>The <code>string.h</code> header in C provides several functions for string manipulation.</p>
            </div>

        </div>

        <div class="slide">
            <img src="assets/or.jpg" alt="">
            <div class="info">
                <h1>String Length</h1>
        <p>The <code>strlen()</code> function is used to determine the length of a string.</p>
        <pre><code>#include &lt;string.h&gt;

char str[] = "Hello";
int length = strlen(str);
printf("Length of string: %d\n", length);</code></pre>
    </section>

    <section class="content">
        
            </div>
        </div>

        <div class="slide">
            <img src="assets/or.jpg" alt="">
            <div class="info">
                
    </section>

    <section class="content">
        <h1>String Manipulation</h1>
        <p>Functions like <code>strcpy()</code>, <code>strcat()</code>, and <code>strncpy()</code> are used for string manipulation.</p>
        <pre><code>#include &lt;string.h&gt;

char dest[20];
char src[] = "World";
strcpy(dest, src); // Copy src to dest
printf("Copied string: %s\n", dest);</code></pre>
            </div>
        </div>

        <div class="slide">
            <img src="assets/1.jpg" alt="">
            <div class="info">
                <h1>String Searching</h1>
        <p>The <code>strstr()</code> function is used to find the first occurrence of a substring within a string.</p>
        <pre><code>#include &lt;string.h&gt;

char str[] = "The quick brown fox";
char *sub = "brown";
char *result = strstr(str, sub);
printf("Substring found at position: %d\n", result - str + 1);</code></pre>
            </div>
        </div>

        <div class="slide">
            <img src="assets/2.jpg" alt="">
            <div class="info">
                <h1>Character Conversion</h1>
        <p>Functions like <code>tolower()</code> and <code>toupper()</code> are used for character case conversion.</p>
        <pre><code>#include &lt;string.h&gt;
#include &lt;ctype.h&gt;

char ch = 'A';
char lower = tolower(ch);
char upper = toupper(ch);
printf("Lowercase: %c\n", lower);
printf("Uppercase: %c\n", upper);</code></pre>
            </div>
        </div>

        <div class="slide">
            <img src="assets/2.jpg" alt="">
            <div class="info">
                <p><strong>Understanding and utilizing the string library functions provided by <code>string.h</code> is essential for efficient string manipulation in C programming!</strong></p>
            </div>
        </div>
        
        <div class="navigation">
            <i class="fas fa-chevron-left prev-btn"></i>
            <i class="fas fa-chevron-right next-btn"></i>
        </div>
        <div class="navigation-visibility">
            <div class="slide-icon active"></div>
            <div class="slide-icon"></div>
            <div class="slide-icon"></div>
            <div class="slide-icon"></div>
            <div class="slide-icon"></div>
            <div class="slide-icon"></div>
            <div class="slide-icon"></div>

        </div>
    </div>

</div>
<div class="play-button">
    <button onclick="window.location.href='unit4'">Play</button>
  </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const nextBtn = document.querySelector(".next-btn");
            const prevBtn = document.querySelector(".prev-btn");
            const slides = document.querySelectorAll(".slide");
            const slideIcons = document.querySelectorAll(".slide-icon");
            let slideNumber = 0;

            const showSlide = (n) => {
                slides.forEach((slide) => {
                    slide.classList.remove("active");
                });
                slideIcons.forEach((icon) => {
                    icon.classList.remove("active");
                });

                if (n < 0) {
                    slideNumber = slides.length - 1;
                } else if (n >= slides.length) {
                    slideNumber = 0;
                } else {
                    slideNumber = n;
                }

                slides[slideNumber].classList.add("active");
                slideIcons[slideNumber].classList.add("active");
            };

            nextBtn.addEventListener("click", () => {
                showSlide(slideNumber + 1);
            });

            prevBtn.addEventListener("click", () => {
                showSlide(slideNumber - 1);
            });
        });
    </script>

</body>

</html>